
                                                                      # blog

 Once the files are downloaded, open each file individually. Then, within the terminal, type the command `npm install` for both the frontend and backend. After that, run the command `npm run dev` for each file.

                                                                      
