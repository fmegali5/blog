# 📰 مدونة إخبارية - Frontend (React)

تطبيق React كامل لمدونة إخبارية مع نظام إدارة للأدمن والكتّاب والناشرين.

## 🚀 المميزات

- ✅ واجهة مستخدم عصرية وسريعة مع React
- ✅ نظام تسجيل دخول وإنشاء حسابات
- ✅ لوحات تحكم منفصلة لكل دور (Admin, Writer, Publisher)
- ✅ عرض المقالات مع التصفية حسب الأقسام
- ✅ صفحة تفاصيل المقال
- ✅ إنشاء وتعديل المقالات
- ✅ لوحة إدارة المستخدمين (Admin)
- ✅ لوحة إدارة الأقسام (Admin)
- ✅ لوحة إدارة الاستطلاعات (Admin)
- ✅ تصميم متجاوب يعمل على جميع الأجهزة

## 📋 المتطلبات

- Node.js (v14 أو أحدث)
- npm أو yarn
- Backend API يعمل على المنفذ 5000

## ⚙️ التثبيت

### 1. تثبيت الحزم

```bash
cd frontend
npm install
```

### 2. تشغيل التطبيق

```bash
npm run dev
```

التطبيق سيعمل على: `http://localhost:3000`

## 🏗️ هيكل المشروع

```
frontend/
├── public/                    # الملفات العامة
├── src/
│   ├── components/           # المكونات المشتركة
│   │   ├── Navbar.jsx       # شريط التنقل
│   │   ├── Navbar.css
│   │   └── PrivateRoute.jsx # حماية المسارات
│   ├── context/             # Context API
│   │   └── AuthContext.jsx # إدارة المصادقة
│   ├── pages/               # الصفحات
│   │   ├── Login.jsx        # تسجيل الدخول
│   │   ├── Register.jsx     # إنشاء حساب
│   │   ├── Home.jsx         # الصفحة الرئيسية
│   │   ├── ArticleDetail.jsx # تفاصيل المقال
│   │   ├── MyArticles.jsx   # مقالاتي
│   │   ├── CreateArticle.jsx # إنشاء مقال
│   │   ├── EditArticle.jsx  # تعديل مقال
│   │   ├── dashboards/      # لوحات التحكم
│   │   │   ├── AdminDashboard.jsx
│   │   │   ├── WriterDashboard.jsx
│   │   │   └── PublisherDashboard.jsx
│   │   └── admin/           # صفحات الإدارة
│   │       ├── ManageUsers.jsx
│   │       ├── ManageCategories.jsx
│   │       └── ManagePolls.jsx
│   ├── App.jsx              # المكون الرئيسي
│   ├── main.jsx             # نقطة البداية
│   └── index.css            # الأنماط العامة
├── index.html
├── vite.config.js           # إعدادات Vite
└── package.json
```

## 🔐 المصادقة

التطبيق يستخدم JWT للمصادقة. التوكن يتم تخزينه في `localStorage` ويُرسل مع كل طلب API.

### تسجيل الدخول

```javascript
const { login } = useAuth()
await login(email, password)
```

### تسجيل الخروج

```javascript
const { logout } = useAuth()
logout()
```

## 🎨 التخصيص

### الألوان

يمكن تخصيص الألوان من ملف `src/index.css`:

```css
:root {
  --primary-color: #D32F2F;
  --secondary-color: #1976D2;
  --success-color: #388E3C;
  /* ... */
}
```

### الخطوط

الخط المستخدم: **Cairo** من Google Fonts

## 📱 الصفحات والمسارات

### صفحات عامة

- `/` - الصفحة الرئيسية (عرض المقالات المنشورة)
- `/login` - تسجيل الدخول
- `/register` - إنشاء حساب جديد
- `/article/:id` - تفاصيل المقال

### صفحات محمية - Admin

- `/admin/dashboard` - لوحة التحكم
- `/admin/users` - إدارة المستخدمين
- `/admin/categories` - إدارة الأقسام
- `/admin/polls` - إدارة الاستطلاعات

### صفحات محمية - Writer

- `/writer/dashboard` - لوحة التحكم
- `/articles/my-articles` - مقالاتي
- `/articles/create` - كتابة مقال جديد
- `/articles/edit/:id` - تعديل مقال

### صفحات محمية - Publisher

- `/publisher/dashboard` - لوحة التحكم
- `/articles/my-articles` - جميع المقالات
- `/articles/create` - إنشاء مقال

## 🔌 الاتصال بـ Backend API

التطبيق يستخدم `axios` للاتصال بـ Backend API. الإعدادات موجودة في `vite.config.js`:

```javascript
export default defineConfig({
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:5000',
        changeOrigin: true
      }
    }
  }
})
```

## 🧩 المكونات الرئيسية

### AuthContext

يوفر:
- `user` - بيانات المستخدم الحالي
- `loading` - حالة التحميل
- `login()` - تسجيل الدخول
- `register()` - إنشاء حساب
- `logout()` - تسجيل الخروج
- `isAuthenticated` - هل المستخدم مسجل دخول
- `isAdmin` - هل المستخدم أدمن
- `isWriter` - هل المستخدم كاتب
- `isPublisher` - هل المستخدم ناشر

### PrivateRoute

يحمي المسارات ويتحقق من:
- هل المستخدم مسجل دخول
- هل لديه الصلاحيات المطلوبة

```jsx
<PrivateRoute roles={['admin']}>
  <AdminDashboard />
</PrivateRoute>
```

### Navbar

شريط تنقل يعرض:
- لوحة التحكم (حسب دور المستخدم)
- معلومات المستخدم
- أزرار تسجيل الدخول/الخروج

## 📦 الحزم المستخدمة

- **react** - مكتبة UI
- **react-router-dom** - التنقل بين الصفحات
- **axios** - طلبات HTTP
- **react-toastify** - إشعارات Toast
- **react-quill** - محرر نصوص غني
- **react-icons** - أيقونات
- **date-fns** - معالجة التواريخ
- **vite** - أداة البناء

## 🚀 البناء للإنتاج

```bash
npm run build
```

سيتم إنشاء مجلد `dist` يحتوي على الملفات الجاهزة للرفع.

### معاينة البناء

```bash
npm run preview
```

## 🎯 خصائص إضافية

### تصفية المقالات

يمكن تصفية المقالات حسب:
- القسم
- الحالة
- الكاتب
- البحث في العنوان والملخص

### رفع الصور

لرفع الصور، يمكنك استخدام:
- Cloudinary (مُعد مسبقاً في Backend)
- أي خدمة رفع صور أخرى

### إشعارات الوقت الفعلي

يمكن إضافة Socket.io للحصول على إشعارات فورية عند:
- نشر مقال جديد
- قبول/رفض مقال
- تعليقات جديدة

## 🐛 استكشاف الأخطاء

### خطأ CORS

إذا واجهت مشكلة CORS:
1. تأكد من تشغيل Backend على المنفذ 5000
2. تحقق من إعدادات CORS في Backend

### خطأ في تحميل الصور

تأكد من:
- رفع الصور بشكل صحيح
- استخدام URLs كاملة للصور

## 📞 الدعم

للمساعدة أو الأسئلة، افتح Issue في المشروع.

---

Made with ❤️ for Arabic News Blogs
